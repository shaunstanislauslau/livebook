{
  "stack": "heroku-18",
  "buildpacks": [
    {
      "url": "https://github.com/HashNuke/heroku-buildpack-elixir.git"
    },
    {
      "url": "https://github.com/gjaldon/heroku-buildpack-phoenix-static.git"
    }
  ],
  "env": {
    "APP_NAME": {
      "description": "The Heroku app name. You can change it later with `heroku config:set APP_NAME=new_app_name`.",
      "required": true
    },
    "LIVEBOOK_PASSWORD": {
      "description": "Password for the HTTP Basic Auth. The username is 'app'.",
      "generator": "secret"
    }
  }
}
